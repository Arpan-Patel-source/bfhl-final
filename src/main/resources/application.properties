server.port=${PORT:8080}
spring.application.name=bfhl

# ── YOUR DETAILS ─────────────────────────────────────────────────────────────
# Set these as Environment Variables in Render dashboard.
# Format: APP_USER_FULL_NAME=your_name  (lowercase, underscore between names)
#         APP_USER_DOB=ddmmyyyy
# The defaults below are placeholders only.
app.user.full-name=${APP_USER_FULL_NAME:john_doe}
app.user.dob=${APP_USER_DOB:17091999}
app.user.email=${APP_USER_EMAIL:john@xyz.com}
app.user.roll-number=${APP_USER_ROLL_NUMBER:ABCD123}
